# terminyl

WIP terminal-native document typesetter with Typst-inspired syntax.

Structured documents are rendered using ASCII and Unicode glyphs for display in plain text terminals.


## Example

An example input file can be found at `src/test.termy`. Running `build/terminyl src/test.termy` from the top level folder will write the structured output to `stdout`.