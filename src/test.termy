= Project Status Report

Last updated: _January 2026_

This project demonstrates a *terminal-first markup language* with 
expression evaluation. Text wraps naturally to fit the terminal 
width, making it ideal for generating reports, documentation, or 
any formatted output directly in the shell.

== Key Features

- Multi-level headings with box-drawing characters
- Inline formatting: *bold*, _italic_, and `code spans`
- Expression evaluation for dynamic content

== Budget Analysis

Initial budget: $#(10000)
Expenses to date: $#(7350)
Remaining: $#(10000 - 7350)
Burn rate: #((7350 / 10000) * 100)%

Run with: `terminyl report.termy`